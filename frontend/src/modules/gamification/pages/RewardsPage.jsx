// TODO (Person 4): Implement Rewards page
// - Fetch GET /api/gamification/rewards
// - Show user's current point balance
// - Reward cards with stock + pointsRequired
// - Redeem button → POST /api/gamification/rewards/:id/redeem
//   Backend atomically deducts employee.points + reward.stock via Mongoose session
// - ADMIN: Create reward modal

export default function RewardsPage() {
  return <div className="p-6 text-slate-400">TODO: Build RewardsPage</div>;
}
