const express = require('express');
const router = express.Router();

// TODO (Person 3): Mount governance routes
// GET  /policies
// POST /policies                          (ADMIN)
// POST /policies/:id/acknowledge          (all)
// GET  /policies/:id/acknowledgements     (ADMIN, MANAGER)
// GET  /audits
// POST /audits                            (ADMIN, MANAGER)
// GET  /compliance-issues
// POST /compliance-issues                 (ADMIN, MANAGER)
// PATCH /compliance-issues/:id/resolve    (ADMIN, MANAGER)

module.exports = router;
