// TODO (Person 1): Implement environmental service
// - getEmissionFactors()
// - createEmissionFactor(data)
// - getTransactions(query)
// - createTransaction({ departmentId, emissionFactorId, quantity, source, date })
//   Business rule: calculatedEmission = quantity * emissionFactor.factorValue (compute here, never on client)
// - getGoals()
// - createGoal({ title, targetValue, deadline, departmentId })
// - updateGoalProgress(goalId, currentValue)

module.exports = {};
