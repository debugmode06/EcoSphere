// TODO (Person 2): Implement social service
// - getActivities()
// - createActivity(data)
// - getParticipations(query)
// - joinActivity({ employeeId, activityId, proofUrl })
// - approveParticipation(participationId, { approvalStatus })
//   Business rule: cannot APPROVE if proofUrl empty and activity.evidenceRequired = true

module.exports = {};
